/* Дано натуральное число. Вывести все значения от 1 до N.
*/

int numbers = 45;
int count = 1;
while (count <= numbers)
{
    Console.WriteLine(count);
    //Console.Write(+count + " ");
    count = count + 1;
}

